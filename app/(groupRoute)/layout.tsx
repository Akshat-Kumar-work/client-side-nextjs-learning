
const layout = ({children}:{children:React.ReactNode}) => {
  return (
    <div>
        <div className=" flex items-center justify-center bg-orange-500">
        layout of group route folder having signin and signup routes
            </div>
        {children}
    </div>
  )
}

export default layout